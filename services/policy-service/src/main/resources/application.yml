# ============================================================
# 📋 POLICY SERVICE - Specific Configuration
# ============================================================
# Imports shared configuration from shared-kernel
# ============================================================

spring:
  config:
    import: "classpath:/application-shared.yml"
  application:
    name: policy-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:policy_db}
    username: ${DB_USER:assureflow}
    password: ${DB_PASSWORD:assureflow123}
  kafka:
    consumer:
      group-id: policy-service-group

# ============================================================
# 🔗 Feign Client Configuration (Service-Specific)
# ============================================================
feign:
  client:
    config:
      client-service:
        url: ${FEIGN_CLIENT_CONFIG_CLIENT-SERVICE_URL:http://localhost:8084}
  circuitbreaker:
    enabled: true

# ============================================================
# 🛡️ Resilience4j (Service-Specific Circuit Breakers)
# ============================================================
resilience4j:
  circuitbreaker:
    instances:
      client-service:
        register-health-indicator: true
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
  retry:
    instances:
      client-service:
        max-attempts: 3
        wait-duration: 500ms

# ============================================================
# 📚 SpringDoc (Service-Specific Package Scan)
# ============================================================
springdoc:
  packages-to-scan: com.pfe.policy.infrastructure.web.controller

# ============================================================
# 🌐 Server Configuration
# ============================================================
server:
  port: 8086

# ============================================================
# 🐳 Docker Profile Overrides (Service-Specific)
# ============================================================
---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres-db:5432/policy_db}
    username: ${SPRING_DATASOURCE_USERNAME:assureflow}
    password: ${SPRING_DATASOURCE_PASSWORD:assureflow_secure_2024}
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka:29092}
server:
  port: 8080

feign:
  client:
    config:
      client-service:
        url: http://client-service:8080
